server:
  port: 8080

spring:
  application:
    name: currency-converter
  datasource:
    url: jdbc:postgresql://localhost:5432/currencydb
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update  

api:
  key:
    path: api_key.txt

currency:
  codes:
    path: currency_codes.txt  